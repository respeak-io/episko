# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store/
dist
dist-ssr
*.local

# Azure signing coordinates + service-principal creds (never commit) — see src-tauri/SIGNING.md
.env
.env.signing

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP session artifacts (page snapshots / console logs from browser-driven
# profiling runs). Scratch output, never part of the project.
.playwright-mcp/

# Coverage output (`pnpm coverage`). A yardstick, not an artifact. cargo llvm-cov
# writes under src-tauri/target/, already ignored by src-tauri/.gitignore.
coverage/

# generated Artifact bundle for site/index.html (not deployed, not source)
build/

# wrangler
.wrangler/
